<div class="vh-100 text-center text-white d-flex flex-column justify-content-center align-items-center">
  <div class="mt-3 card border border-dark">
    <div class="card-body">
      <div class="card-title">
        <h1>Editing Tweet</h1>
      </div>
      <div class="card-text text-break p-1 mt-4 mb-2">
        <%= render 'form', tweet: @tweet %>
      </div>
      <%= link_to 'Show', @tweet, class:"card-link btn mb-1 btn-primary" %> |
      <%= link_to 'Back', tweets_path, class:"card-link btn mb-1 btn-secondary" %>
    </div>
  </div>
