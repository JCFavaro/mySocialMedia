<div class="h-100 text-center text-white d-flex flex-column justify-content-center align-items-center">
  <div class="card border border-dark">
    <div class="card-body">
    <div class="card-title border-bottom border-success">
      My <%= @tweet.id %> tweet created
    </div>
      <div class="card-text text-break p-2 mb-2 singularTweet">
        <%= @tweet.body%>
      </div>
      <%= link_to 'Edit', edit_tweet_path(@tweet), class:"btn btn-warning" %> |
      <%= link_to 'Back', tweets_path, class:"btn btn-primary" %>
    </div>
  </div>
